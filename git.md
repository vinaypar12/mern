git init - initialis
git add <filename>
git add . add all files current dir to stage
git restore filename
git restore --staged filename
git checkout -b <branch name> - create a new branch
git checkout branch - checkout
git branch - list branches
git branch -d <branch name> - delete branch
git commit -m "commit message"
git push - push the change
git pull - pull the change
git clone <repo link> - clone a repository
git status - check status
git log - view commit history
git diff - view changes
git remote -v - view remote repository
git fetch - fetch changes from remote
git merge <branch name> - merge branch into current branch
git rebase <branch name> - rebase current branch onto another branch
git stash - stash changes
git reflog - give all changes checkout commit etc
git reset --hard <commit id> - reset to a specific commit
git tag <tag name> - create a tag
git show <commit id/tag name> - view details of a commit or tag
git clean -f - remove untracked files
git config --global user.name "Your Name" - set global username
git config --global user.email "
git remote add origin <repo>
git push -u origin bname - 1st only
